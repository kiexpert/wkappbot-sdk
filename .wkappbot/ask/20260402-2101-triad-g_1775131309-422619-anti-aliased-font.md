# Triad Debate

> **Question**: [G:1775131309.422619] Anti-aliased font OCR correction strategy debate (정반합):

Problem: OCR mismatches on anti-aliased/ClearType fonts in HTS/MFC apps. Sub-pixel rendering shifts (0.3px), DPI scaling, and ClearType variations make exact pixel-hash matching unreliable for a correction dictionary.

정: Exact pixel-hash → correct-char mapping (fast lookup, zero tolerance)
반: Fuzzy perceptual hash (pixel block average → bit vector → Hamming distance threshold)

Question: Which is better for building a self-learning OCR correction cache in an Experience DB? Is there a better synthesis? Consider: MFC bitmap fonts vs ClearType, per-app/per-font isolation, storage cost, lookup speed, false positive risk.
> **Started**: 2026-04-02 21:01

---


### Gemini (21:02)

Gemini의 응답TITLE: Anti-Aliased OCR Correction Synthesis
FILE_TITLE: ocr_correction_strategy_synthesis(正) Exact Pixel-Hash: Bit-Perfect LookupPros:Zero False Positives: If the hash matches, the character is guaranteed to be correct.Speed: O(1) lookup via hash table/dictionary. Extremely low CPU overhead.Cons:Brittle: Fails on any environmental shift (DPI, ClearType gamma,

> **🦊 Gemini(EXPLORER)** (21:02): Gemini의 응답TITLE: Anti-Aliased OCR Correction Synthesis
FILE_TITLE: ocr_correction_strategy_synthesis(正) Exact Pixel-Hash: Bit-Perfect LookupPros:Zero False Positives: If the hash matches, the characte...


### Gemini (21:02)

OS theme changes).Storage Explosion: Requires separate entries for every sub-pixel shift and font smoothing variation.(反) Fuzzy Perceptual Hash: Geometric AbstractionPros:Robustness: Handles anti-aliasing artifacts and minor rendering noise by focusing on "structure" over "pixels."Generalization: One hash can represent multiple rendering variations of the same glyph.Cons:Collisions: High risk of false positives between similar characters (e.g., 'I', 'l', '1' or 'O', '0') at small font sizes.Compute Cost: Calculating Hamming distance/Bit-vectors for every OCR candidate is slower than a direct hash lookup.(合) Synthesis: Multi-Stage Normalized Feature Vector (MFV)The optimal synthesis for MFC/HTS (Home Trading System) environments—where fonts are often small and tightly packed—is Normalized Feature Vectoring with Contextual Clustering.1. Binarization via

> **🦊 Gemini(EXPLORER)** (21:02): OS theme changes).Storage Explosion: Requires separate entries for every sub-pixel shift and font smoothing variation.(反) Fuzzy Perceptual Hash: Geometric AbstractionPros:Robustness: Handles anti-alia...


### Gemini (21:02)

Adaptive Thresholding (Standardization)Before hashing, apply Otsu’s Binarization or a local adaptive threshold to the anti-aliased bitmap. This strips the ClearType sub-pixel color channels, converting the grayscale/colored edges into a clean 1-bit mask.2. The Experience DB Layering (Tiered Search)Tier 1 (Exact Match): Match against a normalized 1-bit hash. This handles 90% of static UI elements.Tier 2 (Structural Centroids): If Tier 1 fails, use Zernike moments or Hu moments. These are invariant to scale and minor shifts, capturing the "essence" of the character shape rather than pixel placement.Tier 3 (Contextual Verification): Use the H

> **🦊 Gemini(EXPLORER)** (21:02): Adaptive Thresholding (Standardization)Before hashing, apply Otsu’s Binarization or a local adaptive threshold to the anti-aliased bitmap. This strips the ClearType sub-pixel color channels, convertin...


### Gemini (21:02)
YOU MUST 위 삼두 답변을 꼼꼼히 읽고 반드시 한국어로 종합 의견을 작성하세요! 각 AI별 핵심 차이점과 본인 의견 포함 → 슬랙 쓰레드에 reply: wkappbot slack reply "종합의견..." --msg 1775131309.422619

TS app’s metadata (e.g., "This field only contains numbers") to prune the search space, eliminating the "O vs 0" ambiguity inherent in fuzzy hashing.3. Per-App Isolation & Experience DB TrainingInstead of a global dictionary, use Process-Name + Font-Height as the primary key. MFC apps often use non-standard rendering pipelines; isolation prevents "bleed-through" corrections from a different app's font engine.Comparison TableMetricPixel-Hash (정)Perceptual (반)Synthesis (合)DPI SensitivityCritical FailureModerateImmune (Normalized)False Positives0%5–15%< 1% (Context Aided)Storage CostHigh (Redundant)LowOptimal (De-duplicated)Lookup SpeedInstantModerateFast (Two-stage)Synthesis Recommendation:
Use Normalized 1-bit Hashing as the primary cache key. When a "New" pattern

> **🦊 Gemini(EXPLORER)** (21:02): TS app’s metadata (e.g., "This field only contains numbers") to prune the search space, eliminating the "O vs 0" ambiguity inherent in fuzzy hashing.3. Per-App Isolation & Experience DB TrainingInstea...


### Gemini (21:02)

e Experience DB. This creates a "Self-Learning" system that bridges the gap between the speed of 정 and the flexibility of 반.Are you seeing specific character pairs (like '8' and 'B') failing more often due to ClearType bleeding in your HTS application?

> **🦊 Gemini(EXPLORER)** (21:02): e Experience DB. This creates a "Self-Learning" system that bridges the gap between the speed of 정 and the flexibility of 반.Are you seeing specific character pairs (like '8' and 'B') failing more ofte...

> **🦉 Moderator** (21:02): 📩 *[DM→gpt]* [MODERATOR]: 🦊 Gemini(EXPLORER) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (21:02): 📩 *[DM→claude]* [MODERATOR]: 🦊 Gemini(EXPLORER) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (21:02): ⏰ *🦊 Gemini(EXPLORER)* finished! Moderator: wrap up, 2 AI(s) remaining.

> **🦉 Moderator** (21:02): 📢 *[Moderator→ALL]* [MODERATOR]: Once all answers are in, we begin Round 1 of 정반합 debate. Prepare your [DEBATE_JSON] with STANCE points.

> **🚧 Claude** (21:02): TITLE: Anti-Aliased OCR Correction Cache — 정반합 [G:1775131309.422619]
FILE_TITLE: ocr_correction_cache_jeongbanhap_G1775131309

[G:1775131309.422619] 합 — Agree with Gemini's direction, with two sharpening points:
On Otsu binarization before hashing: Correct call. Strips ClearType color fringe, makes the hash DPI/rendering-agnostic. Only caveat: Otsu assumes bimodal histogram — works well for high-contrast HTS bitmap fonts, may struggle on low-contrast gray-on-gray MFC status bars. Add a fallback to Sauvola (already in WKAppBot's CCA pipeline) for those edge cases. Reuse existing infrastructure, zero extra cost.
On tiered ExperienceDB (T1 exact → T2 fuzzy): Solid. One concrete addition — store the binarized bitmap as the hash input, not the raw ClearType bitmap. This means T1 exact hash hits survive DPI scaling and font smoothing changes across sessions, which is the original failure mode. Raw pixel hash in 정 fails precisely because it hashes pre-binarization noise.
On per-app isolation (ProcessName + FontHeight key): Strong. Extend the key to ProcessName + FontHeight + FontName where extractable via GetTextFace / UIA Font properties. Kiwoom and Cybos both mix bitmap and GDI ClearType fonts within the same window — same height, different metric — so FontName breaks the remaining collision cases.
The one gap Gemini's outline has: No mention of write-back confidence gating. Fuzzy T2 hits should not auto-promote to T1 until confirmed by N≥3 consistent matches (same binarized hash → same correction across sessions). Otherwise one bad fuzzy match poisons the exact cache permanently. Add a hit_count + confirmed flag to the ExperienceDB schema.
Summary verdict: Gemini's layered approach is the right synthesis. Sharpen with: Sauvola fallback for low-contrast, binarize-before-hash for DPI stability, extend isolation key with FontName, and gate T1 promotion behind confirmation count.

> **claude** (21:02): 🔀 *[claude]* Cross-prompt sent (506 chars)

> **🦉 Moderator** (21:02): 📩 *[DM→gpt]* [MODERATOR]: 🤖 Claude(AUDITOR) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (21:02): ⏰ *🤖 Claude(AUDITOR)* finished! Moderator: wrap up, 1 AI(s) remaining.

> **🦉 Moderator** (21:02): 📢 *[Moderator→ALL]* [MODERATOR]: Once all answers are in, we begin Round 1 of 정반합 debate. Prepare your [DEBATE_JSON] with STANCE points.

> **🚧 ChatGPT** (21:04): Short answer:

정 (Exact pixel-hash) → too brittle for ClearType/DPI variation

반 (Perceptual hash) → usable but risks false positives

합 (Best) → multi-key, hierarchical matching (exact → perceptual → structural + context gating)

정 (Exact Pixel-Hash)

Pros

O(1) lookup, extremely fast

Zero ambiguity when match exists

Minimal compute at runtime

Cons

Breaks under:

ClearType subpixel shifts (~0.3px)

DPI scaling / font hinting differences

rendering backend differences (GDI vs DirectWrite)

Cache fragmentation: same glyph → many hashes

Poor reuse across sessions/machines

👉 Verdict: Only useful as a first-pass fast path, not a primary strategy

반 (Fuzzy Perceptual Hash)

Pros

Robust to:

anti-aliasing

small geometric shifts

grayscale variation

Better generalization across environments

Enables clustering of “same glyph variants”

Cons

False positives (e.g., ‘8’ vs ‘B’, ‘O’ vs ‘0’)

Requires:

threshold tuning

collision handling

Slightly slower (Hamming distance comparisons)

Still weak on heavily distorted ClearType subpixel color patterns

👉 Verdict: Good core matching layer, but unsafe alone

핵심 현실 문제

ClearType is subpixel RGB-based, not grayscale → traditional perceptual hashes lose signal

MFC apps often mix:

bitmap fonts (stable)

GDI/ClearType text (unstable)

OCR errors are systematic, not random → exploitable with context

합 (Recommended Synthesis: Multi-Layer Matching Pipeline)
1. Tier 0: Exact Hash (Fast Path)

Key: (app_id, font_signature, dpi, glyph_bbox_hash)

If hit → return immediately

👉 Handles stable cases (bitmap fonts, repeated UI)

2. Tier 1: Perceptual Hash (Candidate Generation)

Use:

grayscale + optionally RGB-channel-aware hash

Retrieve top-N candidates within Hamming threshold

👉 Expands recall under rendering variation

3. Tier 2: Structural Similarity Filter

Refine candidates using:

stroke topology (skeletonization)

edge map comparison

projection histograms (vertical/horizontal)

👉 Kills “8 vs B” type errors

4. Ti...

> **앱봇아이** (21:04): 🔔 *삼두 완료* — 종합의견 리마인더 예약됨 (5s)
📌 `--msg 1775131309.422619`

