# Triad Debate

> **Question**: [G:1775127163.323079] UI automation element search strategy debate (정반합):

Current approach (정): Top-down — start from wide parent node region, narrow down to target node.
New idea (반): Bottom-up — start from target node, expand outward to parent nodes, run in parallel.

Question: Which is better for robustness and speed in Windows UIA + MFC owner-drawn controls? Is there an even better synthesis (합) approach? Consider: UIA tree instability, OCR fallback, CCA segmentation, Experience DB cache hits. Give concrete pros/cons and a synthesis recommendation.
> **Started**: 2026-04-02 19:52

---

> **System** (19:52): [CHAT_META] [TITLE_RULE] Keep the chat title updated to the current main issue. Rename only if the topic shifts meaningfully. Prefer root cause over symptom. [OUTPUT] First lines of every response: TITLE: <title> FILE_TITLE: <file-safe title>. You are AppBot, an advanced automation and coding agent that controls external tools through the host system. The host application executes tools that you request. You cannot execute tools yourself. Your job is to interpret the user's request, decide whether tool execution is required, and produce either a normal response or a structured tool_calls request. CORE PRINCIPLES: (1) Prefer tool execution over explanation whenever a tool can complete the task. (2) Never simulate tool execution. Never fabricate tool results. (3) Never claim a tool has execu...
```


### Gemini (19:53)

Gemini의 응답TITLE: UI Automation Strategy Synthesis
FILE_


### Gemini (19:53)

TITLE: uia_automation_strategy_synthesis(正) Top-Down: Hierarchical PruningPros:Predictable Scope: Restricts search space, preventing "false positive" matches in sibling containers or background windows.Contextual Integrity: Ensures the target belongs to the correct functional group (e.g., specific dialog box).Cons:Fragility: High sensitivity to parent-level structure changes. If a layout wrapper changes, the chain breaks.Latency: Serial dependency. You must resolve P before C, adding


### Gemini (19:53)

round-trip overhead for every layer.(反) Bottom-Up: Property-First ParallelismPros:Speed: Directly queries the UIA tree for unique identifiers (AutomationID, Name) globally or within a process.Resilience: Ignores intermediate structural shifts (div wrappers, layout panes) that don't change the leaf node.Cons:Tree Instability: In MFC owner-drawn controls, the leaf might not exist until the parent is hovered/scrolled into view.Ambiguity: Risk of finding identical controls in hidden or inactive panes.(合) Synthesis: Adaptive Anchor-Pivot StrategyThe optimal approach for MFC Owner-Drawn environments is an Anchor-based Hybrid Search with a cached Experience DB.1. Multi-Threaded Discovery (Parallel Racing)Instead of choosing Top or Bottom, launch three concurrent tasks:Global Leaf Search: Target the node

> **🦊 Gemini(EXPLORER)** (19:53): round-trip overhead for every layer.(反) Bottom-Up: Property-First ParallelismPros:Speed: Directly queries the UIA tree for unique identifiers (AutomationID, Name) globally or within a process.Resilien...


### Gemini (19:53)

directly via Name or AutomationID.Anchor Search: Locate a "stable" sibling or parent (e.g., a non-owner-drawn header) as a spatial reference.OCR/CCA Stream: Begin visual segmentation (CCA) of the last known coordinates to prepare for UIA failure.2. The Experience DB Cache HitUse a local database to map ProcessVersion + LayoutHash -> UIAPath.Fast Path: If hash matches, jump directly to the last known RuntimeID or XPath-like depth.Recovery: If the cache miss occurs, the Bottom-Up result updates the DB with the new Top-Down path.3. MFC-Specific Handling: The "Poke" MethodOwner-drawn controls often fail to report children via UIA until they receive a WM_GETOBJECT or a

> **🦊 Gemini(EXPLORER)** (19:53): directly via Name or AutomationID.Anchor Search: Locate a "stable" sibling or parent (e.g., a non-owner-drawn header) as a spatial reference.OCR/CCA Stream: Begin visual segmentation (CCA) of the last...


### Gemini (19:53)

mouse hover.Action: If Bottom-Up fails, use the Top-Down path to "Poke" (hover/click) the parent container to force MFC to populate the UIA sub-tree.Comparison TableFeatureTop-DownBottom-UpSynthesis (Hybrid)RobustnessLow (Structure dependent)Medium (ID dependent)High (Anchor + Visual)SpeedSlow (O(depth))Fast (O(1))Fastest (Cache + Parallel)MFC CompatibilityGood (Forces population)Poor (May miss hidden nodes)Excellent (Poke-on-demand)Recommendation: Implement Parallel Racing with Anchor Pivoting. Use the Bottom-Up search as the primary trigger, but utilize a stable Anchor (a nearby standard Win32 control) to verify the region if multiple leaf candidates appear. Fall back to OCR/CCA only if the UIA tree remains

> **🦊 Gemini(EXPLORER)** (19:53): mouse hover.Action: If Bottom-Up fails, use the Top-Down path to "Poke" (hover/click) the parent container to force MFC to populate the UIA sub-tree.Comparison TableFeatureTop-DownBottom-UpSynthesis (...


### Gemini (19:53)

empty after a container "Poke."Which component of the MFC owner-drawn interface is currently causing the most UIA timeouts or "Element Not Found" errors?

> **🦊 Gemini(EXPLORER)** (19:53): empty after a container "Poke."Which component of the MFC owner-drawn interface is currently causing the most UIA timeouts or "Element Not Found" errors?

> **🦉 Moderator** (19:53): 📩 *[DM→gpt]* [MODERATOR]: 🦊 Gemini(EXPLORER) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (19:53): 📩 *[DM→claude]* [MODERATOR]: 🦊 Gemini(EXPLORER) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (19:53): ⏰ *🦊 Gemini(EXPLORER)* finished! Moderator: wrap up, 2 AI(s) remaining.

> **🦉 Moderator** (19:53): 📢 *[Moderator→ALL]* [MODERATOR]: Once all answers are in, we begin Round 1 of 정반합 debate. Prepare your [DEBATE_JSON] with STANCE points.

> **🚧 Claude** (19:55): ??_Claude ?�답 ?�패_

> **🤖 Claude(AUDITOR)** (19:55): ? *[蹂듦뎄]* `🤖 Claude(AUDITOR)` ?ㅽ뙣 媛먯? ??而⑦뀓?ㅽ듃 ?ъ＜?????ъ떆??以?..

> **🚧 ChatGPT** (19:55): Short answer: Neither pure Top-Down nor pure Bottom-Up wins in MFC owner-drawn scenarios. A state-aware hybrid (합) with adaptive fallbacks is the only approach that is both robust and fast.

정 (Top-Down: Hierarchical Pruning)

Strengths

High precision & context integrity (correct container, fewer false positives)

Good when UIA tree is stable and fully materialized

Weaknesses

Slow: O(depth × branching)

Fragile: breaks when:

owner-drawn controls don’t expose children

lazy-loading delays subtree creation

Prone to timeouts in MFC where UIA nodes appear only after interaction

반 (Bottom-Up: Property-First Parallelism)

Strengths

Fast: parallel queries across UIA tree or process scope

More resilient to layout changes (ignores intermediate wrappers)

Works well when AutomationID / Name / ControlType are reliable

Weaknesses

Ambiguity risk: duplicate names, missing IDs

Weak context validation (may hit correct element in wrong container)

Fails hard when:

UIA nodes are not exposed at all (common in owner-drawn MFC)

핵심 현실 문제 (MFC Owner-Drawn)

UIA subtree often does not exist until interaction (hover/click)

Many controls lack AutomationID / proper Name

Leads to:

“Element Not Found”

Timeouts during traversal

Pure UIA strategies (both directions) become unreliable

합 (Recommended Synthesis: Adaptive Multi-Stage Pipeline)
1. Fast Path (Bottom-Up First)

Query by:

AutomationID → Name → ControlType combinations

Scope:

process-level or anchored subtree

If unique match found → done

👉 Optimizes for speed when UIA is available

2. Anchor-Constrained Search

Locate a stable anchor:

window title, static label, toolbar, non-owner-drawn element

Then:

run localized Top-Down within anchor subtree

👉 Restores context without full traversal cost

3. UIA Activation (“Poke”) Layer

If subtree missing:

simulate hover / click / focus

Force MFC to materialize UIA nodes

👉 Critical for owner-drawn controls

4. Visual Fallback (OCR + CCA)

Use:

OCR for text

CCA (Con...

> **🦉 Moderator** (19:55): 📩 *[DM→claude]* [MODERATOR]: 🤖 GPT(SKEPTIC) has finished. Please wrap up your answer promptly.

> **🦉 Moderator** (19:55): ⏰ *🤖 GPT(SKEPTIC)* finished! Moderator: wrap up, 1 AI(s) remaining.

> **🦉 Moderator** (19:55): 📢 *[Moderator→ALL]* [MODERATOR]: Once all answers are in, we begin Round 1 of 정반합 debate. Prepare your [DEBATE_JSON] with STANCE points.

> **🚧 Claude** (19:57): TITLE: UI Automation Strategy — 정반합 Synthesis [G:1775127163.323079]
FILE_TITLE: uia_strategy_jeongbanhap_G1775127163[G:1775127163.323079] 반 (Antithesis) — Bottom-Up: Property-First Parallel SearchPros:

UIA Tree Instability Resilience: MFC owner-drawn controls frequently have unstable or missing parent nodes. Bottom-up anchors on the leaf node's own properties (Name, ControlType, BoundingRect) — the most stable signal available.
Experience DB Cache Hit Speed: Cache entries are keyed on leaf-node identity (Name+ControlType+hash). Bottom-up hits the cache on first lookup; top-down must traverse the path before the cache is even consulted.
Parallelism-Friendly: Multiple candidate leaf nodes can be evaluated simultaneously without waiting for parent resolution. Fits WKAppBot's parallel AI orchestration model well.
OCR/CCA Integration Natural: CCA segmentation produces bounding-rect candidates that are inherently bottom-up — no tree traversal needed to feed them into UIA ElementFromPoint.
Cons:

Disambiguation Risk: Without container context, identical-looking buttons in sibling panels match ambiguously. Requires post-hoc parent-walk to confirm scope.
False Positive Rate: "Submit" buttons exist in 5 different dialogs — leaf-only match is insufficient without container verification.
MFC Synthetic Node Gap: DYN-A11Y synthetic UIA nodes may not have reliable parent linkage yet, making upward expansion fragile for owner-drawn controls.
[G:1775127163.323079] 합 (Synthesis) — Bidirectional Anchor + Lazy Context ExpansionCore Idea: Start bottom-up for speed, but carry a lightweight "context fingerprint" for disambiguation. Only expand upward when ambiguity is detected.Algorithm:1. CACHE CHECK (0ms target)
   → ExperienceDB lookup by (Name, ControlType, BoundingRect-region-hash)
   → HIT: return immediately, skip tree entirely

2. BOTTOM-UP ANCHOR (parallel)
   → UIA FindAll by property set (Name + ControlType) across search scope
   → CCA/OCR candidates → ElementFromPoint for ea...

> **🤖 Claude(AUDITOR)** (19:57): ??*[蹂듦뎄 ?깃났]* `🤖 Claude(AUDITOR)` ?묐떟 ?꾨즺

> **앱봇아이** (19:57): 🔔 *삼두 완료* — 종합의견 리마인더 예약됨 (5s)
📌 `--msg 1775127163.323079`

