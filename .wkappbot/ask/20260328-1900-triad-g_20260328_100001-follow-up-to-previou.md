# 삼두 정반합 회의록

> **질문**: [G:20260328_100001] Follow-up to previous analysis: Gemini proposed that SetConsoleOutputCP in a DETACHED_PROCESS causes a 30s CSRSS/ALPC timeout that blocks the PARENT process handle table and IOCP completions. Claude proposed AppHost 2-stage process + SMB IPC$ timeout. KEY QUESTION: The PARENT is NativeAOT (no .NET, no Console.OutputEncoding). The CHILD calls Console.OutputEncoding. How can the CHILD's SetConsoleOutputCP block the PARENT's GetFileAttributesW and WaitForSingleObject? What kernel mechanism causes a child's console init to block the parent's UNRELATED kernel calls? Is there a shared session lock, ALPC port contention, or job object constraint? Evidence: (1) Parent is NativeAOT, pure Win32 only (2) Parent only calls GetFileAttributesW, WaitForSingleObject, GetQueuedCompletionStatus (3) Child is .NET 8 single-file with Console.OutputEncoding set before command dispatch (4) Same Windows session (5) DETACHED_PROCESS + CREATE_BREAKAWAY_FROM_JOB flags
> **시간**: 2026-03-28 19:00:34
> **상태**: 진행 중...

---

## 🤖 GPT

*대기 중...*

## 💎 GEMINI

**Full Response:**

Gemini의 응답TITLE: Kernel-Level Blocking via CSRSS ALPC and Detached Process Console Initialization
FILE_TITLE: kernel_blocking_csrss_alpc_detached_consoleThe Root Cause: CSRSS ALPC Port Contention and the "Zombie Console" LockThe mechanism causing a NativeAOT Parent to block during a .NET Child's SetConsoleOutputCP (via Console.OutputEncoding) in a DETACHED_PROCESS scenario is likely ALPC (Advanced Local Procedure Call) synchronous exhaustion within the Windows Subsystem Process (CSRSS.exe).When the child calls SetConsoleOutputCP, it triggers an ALPC message to CSRSS to update the console state. Even in a DETACHED_PROCESS, the Windows loader or the .NET runtime often attempts to initialize a "zombie" or "null" console handle if the standard streams are not explicitly redirected to NUL.1. The ALPC / CSRSS Synchronization BottleneckCSRSS manages console handles and code pages session-wide. When the child process requests a code page change:The Synchronous Request: SetConsoleOutputCP is a synchronous ALPC call.The Session Lock: CSRSS often holds a global process/console lock while validating the calling process's credentials and session state.The Parent's Block: Even though the Parent is NativeAOT and "pure Win32," calls like GetFileAttributesW and WaitForSingleObject can involve CSRSS or the I/O Manager if they target symbolic links, networked paths (UNC), or synchronization objects that require subsystem verification.2. The DETACHED_PROCESS ParadoxUsing DETACHED_PROCESS with CREATE_BREAKAWAY_FROM_JOB creates a unique race condition:The child has no console, but .NET 8's Console.OutputEncoding getter/setter forces a GetConsoleWindow or GetStdHandle check.The kernel attempts to resolve the process's connection to the Console Host (conhost.exe). Since it's detached, the request may hang in the kernel-mode ALPC wait state while CSRSS searches for a non-existent console session or times out on a stale handle inherited from the parent's environment block.3. Why GetFileAttributesW BlocksThis is the "Smoking Gun." GetFileAttributesW is not always a simple filesystem metadata read. In a hardened or enterprise environment:Filter Drivers: The call passes through the I/O stack.ALPC Dependency: If the path involves a redirected drive or a specific namespace, the I/O manager may call back into a subsystem.The Handle Table Lock: If the child is still technically in the parent's "spawning" phase (despite DETACHED_PROCESS), the Process Environment Block (PEB) or the Handle Table may be locked by the kernel during the transition. If the child hangs on a CSRSS ALPC timeout, the kernel may keep the parent's process object in a state that stalls non-trivial syscalls.Comparison of MechanismsMechanismProbabilityLogicCSRSS ALPC TimeoutHighChild hangs CSRSS; Parent's subsystem-dependent calls wait on the same ALPC port.Loader Lock ContentionMediumChild's .NET runtime init hits a lock shared via the parent's address space clone.Object Manager HangLowA specific kernel object (Job/Event) is being accessed by both simultaneously.Diagnostic Next StepTo confirm if CSRSS is the bottleneck, we should check the wait chain of the parent process.Would you like me to run a script to capture the Wait Chain (WCT) of the parent process during the 30s hang to see if it is explicitly waiting on csrss.exe?

## 🧠 CLAUDE

*대기 중...*

---
*Auto-generated by WKAppBot Triad — 19:00:34*
