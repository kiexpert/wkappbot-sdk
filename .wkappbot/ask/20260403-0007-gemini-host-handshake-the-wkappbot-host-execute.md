# Ask GEMINI

> **Question**: [HOST-HANDSHAKE]
The WKAppBot host executed a connectivity probe on startup:
[APPBOT_TOOL_CALL_BEGIN]{"id":"tc_init","argv":["readiness"]}[APPBOT_TOOL_CALL_END]
TOOL_RESULTS [MCP+APSP executed_by=host flushed=1 still_running=0]
[{"jsonrpc":"2.0","id":"tc_init","result":{"content":[{"type":"text","text":"Ready: True  host_pid=46436  time=00:06:34  uptime=3743s  mem=2424MB  system_processes=584"}],"isError":false}}]
Host is live. Your TOOL_CALL blocks will be executed in real time.
[/HOST-HANDSHAKE]

Read the file W:/GitHub/WKAppBot/docs/element-search-strategy.md first, then read W:/GitHub/WKAppBot/csharp/src/WKAppBot.CLI/Commands/AnalyzeHackCommand.cs. Based on the strategy document, list concrete improvement suggestions for the hack command's element search logic. Focus on: 1) where to add bottom-up parallel search, 2) where the Poke method fits, 3) Experience DB integration gaps. Keep it concise — bullet points only, no code.
> **Time**: 2026-04-03 00:07:43
> **AI**: gemini

---

## Tool Calls

1. `file read W:/GitHub/WKAppBot/docs/element-search-strategy.md` → ok
2. `file read W:/GitHub/WKAppBot/csharp/src/WKAppBot.CLI/Commands/AnalyzeHackCommand.cs` → ok (8480 bytes)

## Response

### AnalyzeHackCommand Element Search Improvements

**1. Bottom-Up Parallel Search** (line 66)
- Wrap `GetElementAndInfoContainingRectInWindow` + `ExperienceDB.Lookup` in `Task.WhenAny`
- Add process-scope search lane: Name/AutomationId direct query
- Add Anchor-constrained lane: nearest stable Win32 control → search downward only

**2. Poke Method** (after UIA returns empty)
- `PostMessage(WM_GETOBJECT)` or sub-ms hover at (px,py) → force MFC UIA materialization
- Post-poke: retry Bottom-Up + ElementFromPoint lanes only

**3. Experience DB Gaps** (line 141 — save only)
- Add **pre-search lookup** by `(ProcessName, ClassName, RectHash)` before any UIA traversal
- Cache hit → skip `CollectTextLeaves` (line 97)
- Top-Down win → still write back to DB to strengthen Bottom-Up for next run

**4. CCA Enhancement**
- Feed CCA bounding boxes (line 113) directly into parallel `ElementFromPoint` lane
- If UIA empty after Poke → OCR on CCA text regions as visual-only fallback

**Next Steps**
- Refactor to `async/await` + `Task.WhenAny` racing
- Expose Poke from DYN-A11Y to CLI level
- Verify ExperienceDB perf under server mode (line 20)

---
*Generated by WKAppBot agent gemini — 2026-04-03 00:07:43*
