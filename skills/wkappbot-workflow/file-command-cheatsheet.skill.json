{
  "id": "file-command-cheatsheet",
  "app": "wkappbot-workflow",
  "title": "file -- Read/Write/Edit/Grep/Glob Cheatsheet",
  "title_ko": null,
  "desc": "wkappbot file tools for filesystem access. edit auto-creates .bak/ backup. glob MUST use **/ prefix. After edit, U+FFFD corruption triggers auto Slack alert.",
  "steps": [
    "READ: wkappbot file read <path> [--offset N] [--limit N]",
    "GREP: wkappbot file grep \"pattern\" --path dir --type cs [-i] [-C 3] [--max N]",
    "GLOB: wkappbot file glob \"**/*.cs\" --path dir -- ALWAYS use **/ prefix",
    "EDIT: wkappbot file edit <path> --old \"...\" --new \"...\" (auto .bak/ backup)",
    "UNDO: wkappbot file undo <path> -- restore from .bak/",
    "WRITE: wkappbot file write <path> --content \"...\" (.bak/ created on overwrite)",
    "! CP949 files: Korean source with Han comments -- verify encoding before edit",
    "! After edit: U+FFFD detected -> auto Slack alert fired",
    "wkappbot file edit PATH --old-string 'text' --new-string 'text' for INLINE string replacement. --old/--new flags take FILE PATHS (not strings). Without --old-string, value is treated as a file path relative to CWD.",
    "REPLACE-ALL GOTCHA: if --new-string contains --old-string as substring, --replace-all causes recursive double-replacement. Fix: include anchoring context in old-string (e.g. if (argFilter not just argFilter) so the replacement result does NOT contain the old-string."
  ],
  "requirements": null,
  "primary_cmd": null,
  "tags": [
    "file",
    "read",
    "write",
    "edit",
    "grep",
    "glob",
    "encoding",
    "reference",
    "operator"
  ],
  "source_refs": null,
  "regression_script": null,
  "author": "claude",
  "created": "2026-04-07T13:00:00Z",
  "updated": "2026-05-26T01:48:04.0958784Z",
  "version": "1.9"
}