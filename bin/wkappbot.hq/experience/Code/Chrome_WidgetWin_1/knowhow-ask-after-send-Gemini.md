
## FocusStealer @ 2026-04-16 17:42
- Action `ask-after-send-Gemini` on [Chrome_WidgetWin_1] (Code, hwnd=0x204D4) stole focus despite nominally being focusless.
- Next invocation will force yield popup (Win32 prop `WKAppBot_FocusStealer-ask-after-send-Gemini` stamped).
- If this is always the case, prefer wrapping with `--yield` or switching to SendInput approach.

## FocusStealer @ 2026-04-16 21:13
- Action `ask-after-send-Gemini` on [Chrome_WidgetWin_1] (Code, hwnd=0x20040) stole focus despite nominally being focusless.
- Next invocation will force yield popup (Win32 prop `WKAppBot_FocusStealer-ask-after-send-Gemini` stamped).
- If this is always the case, prefer wrapping with `--yield` or switching to SendInput approach.

## FocusStealer @ 2026-04-16 21:18
- Action `ask-after-send-Gemini` on [Chrome_WidgetWin_1] (Code, hwnd=0x20040) stole focus despite nominally being focusless.
- Next invocation will force yield popup (Win32 prop `WKAppBot_FocusStealer-ask-after-send-Gemini` stamped).
- If this is always the case, prefer wrapping with `--yield` or switching to SendInput approach.
