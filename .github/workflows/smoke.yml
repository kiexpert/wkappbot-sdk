name: smoke-csharp

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: windows-latest
    steps:
    - name: Skip automatic smoke
      shell: pwsh
      run: Write-Host "Smoke workflow is manual-only for now."
