---
id: on-load
app: wkappbot-workflow
description: "T1 core tier. Session-start decision rules: when to run mandatory loads, gg trigger, bare-keyword routing. Execution detail -> wkappbot skill read on-load-howto. History -> wkappbot skill read on-load-ref."
tags: [session, compaction, startup, on-load, mandatory, handoff, init, recovery, gg, opus, t1, core, 3tier, router, resume, restart, wake, fresh, boot, ggg, entry, 세션, 시작, 필수, 처음]
---

> **Refresh**: `wkappbot skill read on-load --if-newer` — v1.176 (2026-05-08)

# On Load (T1 core)

## Steps

- `wkappbot skill read on-load --if-newer   # re-read only if the cache changed`

1. WHEN TO RUN THIS SKILL: (A) session start or post-compaction -> run steps 1+2. (B) user sends exactly 'gg' or 'ggg' (case-insensitive, trim whitespace) -> run step 3. (C) user says 'handoff' or 'continue from last session' -> run step 5. (D) user is mid-task or sent a casual greeting -> do NOT run the full pipeline. This skill is an entry-point router, not an always-execute checklist.
2. MANDATORY SKILL LOADS (session start / post-compaction only): run both -- (1) wkappbot skill read suggest-workflow (2) wkappbot skill read ask-suggest-priority-batching. If either fails: skip step 2, report which skill failed, and ask the user to check wkappbot health. Do NOT proceed with suggest triage if either skill is unreadable.
3. CHECK SUGGEST BACKLOG (session start / post-compaction only): wkappbot suggest list. Then follow ask-suggest-priority-batching protocol -- MANDATORY, no shortcuts: (1) merge obvious duplicates. (2) fire wkappbot ask gpt --new-session for batch ranking. (3) STOP and wait for GPT response -- do not burn Sonnet tokens while waiting. (4) per actionable item: fire wkappbot ask gpt with ts+title+root-cause BEFORE any code. (5) only after ask responses arrive: brief Opus agent with ask result + fix path. Fatal = command not found / auth failure. Non-fatal = empty list / timeout. Success: Opus agent reports resolved/skipped counts.
4. GG TRIGGER: if user message is exactly 'gg' or 'ggg' (case-insensitive, trim whitespace -- GG/Gg/ggg all match), locate '## gg Main Workflow' section in project CLAUDE.md and execute every step sequentially without confirmation. Continue on non-fatal step failures; report summary at end. If CLAUDE.md is missing OR has no gg section: stop here and follow step 6 to create it BEFORE executing. Do not re-invoke on-load from within the new section (circular reference).
5. COMPACTION RECOVERY (only after context compaction, not every session start): for full context re-hydration, also run: wkappbot skill read post-compaction-recovery. Skip this step on normal session starts.
6. HANDOFF: if user says 'handoff' or asks to continue from last session, run: wkappbot skill read claude-session-handoff -- then follow its steps to find the previous JSONL and extract context.
7. PROJECT MAIN TASKS -- gg section authoring (run only when step 4 cannot find a gg section): ask the user what the main recurring tasks are, then write '## gg Main Workflow' together in the repo CLAUDE.md. Rules: each step must be an executable shell command or Agent() call -- no vague descriptions. Include a Frequently used skills table (rank/Skill-ID/When). For tag/naming guidance when adding skills to the table, see: wkappbot skill read skill-search-coverage-guide. TEMPLATE (adapt to project): ## gg Main Workflow (MANDATORY when user sends gg or ggg) -- Execute in order: (1) wkappbot skill read on-load (2) wkappbot suggest list (3) Agent(model:opus, prompt:'run wkappbot skill read suggest-workflow first, then triage: <INSERT suggest list output> BUG-AUTO->important->AI-NEWS merge') (4) wkappbot speak triage-done --bg. Skills table: rank1/suggest-workflow/every session, rank2/ask-suggest-priority-batching/every triage.
8. REFLECTION CHAIN: before starting work, check if you hit any recurring pattern this session. If yes, add it to sonnet-bug-stop-policy-ref: wkappbot skill read sonnet-bug-stop-policy-ref. P7=guard-source-first (read guard source at retry #2, not #6). P8=asymmetric-protection (ask: does my guard protect in both directions?). Future Sonnet is watching.
9. SKILL INSTALL CHECK: after any skill contribute/edit this session, verify wkappbot skill install was run. Uninstalled skills are invisible to search and other sessions. Pattern: contribute -> install <path> -> read to verify. See skill-contribute-edit for full rule.
10. TODO: gg Main Workflow master spec. WHEN: user sends exactly 'gg' or 'ggg' at session start. HOW: wkappbot skill read claude-session-handoff then execute steps 0-7 from CLAUDE-ref.md (agent executes 0-6, Opus orchestrates 3-7). WHY: gg cycles are the mandatory triage cadence every session start and post-compaction; workflow covers screen-health audit, skill-lock-audit, cdp-verify, suggest-triage, error-patrol, and ai-news briefing to keep the harness healthy.
11. WHEN TO RUN. (A) session start or post-compaction -> load mandatory skills (see on-load-howto step 1) + run handoff (on-load-howto step 3) + run suggest backlog (on-load-howto step 4). (B) user sends exactly gg or ggg -> run GG TRIGGER (T1 step 2). (C) mid-task or casual greeting -> SKIP full pipeline.
12. GG TRIGGER. User message exactly gg or ggg -> locate the gg Main Workflow section in project root markdown rules file and execute every step sequentially end-to-end NO confirmation, finish with repo-health-doctor. If section missing: ask user for recurring tasks, add the gg Main Workflow section, then run it same session. Quota/limit error: mark handoff_pending, do NOT retry same CLI.
13. BARE KOREAN KEYWORD RULE. If user drops an isolated Korean keyword or short phrase, run wkappbot skill search term FIRST. Never guess intent before the skill search resolves a matching skill.
14. NAVIGATION. Execution details (commands, mandatory loads, agent rules, file-edit discipline, skill-edit discipline, TODO sync) -> wkappbot skill read on-load-howto. History or incident notes or dated bug accumulation -> wkappbot skill read on-load-ref. Both are part of this cluster.
15. SELF-AUDIT POINTER (Opus sessions): at the END of substantive work, before wrapping a session, or after compaction, run the self-reflection audit. Read wkappbot skill read opus-self-reflection (the Opus memoir plus end-of-work checklist: did I skillify what I learned, is it findable, did I tier fat skills, did I avoid bandaids and backdoors, did I propagate to posterity). It pairs with sonnet-bug-stop-policy on the Sonnet side. The whole fleet learns from each session honest reflection.
16. INPUT REFLEXES (bare-key shortcuts -- be precise about single vs double, the user types the Korean giyeok key): EXACTLY ONE giyeok ㄱ sent alone means YES -- accept the assistant most recent proposal and CONTINUE the current in-progress task. Do NOT restart the workflow on a single ㄱ. TWO or more (ㄱㄱ or ㄱㄱㄱ, or gg or ggg) means RESTART -- run the gg Main Workflow in the project CLAUDE.md from the top. Count the giyeok: one is continue, two-plus is restart. If it is ambiguous which the user meant, ASK before restarting, because restart discards the current working context.
17. KNOWLEDGE-IFICATION (jisikhwa) TRIGGER: when the user utters the knowledge-ify keyword (Korean jisikhwa, alone or with a go particle), read knowledge-ification-procedure-jisikhwa and 3-tier-skillify THIS session experience plus process by default (T1 core, T2 howto, T3 ref). Authoritative route is the CLAUDE.md jisikhwa Shortcut (global + project). Concept defs: ki-concept-definitions. Do not flounder; skill search jisikhwa also finds it.
18. LOW-PACE DEFERRED-BUILD EXECUTION (2026-06-04): harness builds get deferred when pace runs over the prorated target (this session ran ~3x ahead of pace, throttling all Edit/Write even for subagents). RESUME them only when pace is UNDER target -- a low-usage session, or later in the week once the prorated target rises above current usage. NOTE: the Thursday 07:00 KST reset alone is NOT the trigger -- a fresh week front-loaded by a heavy session is still over-pace. When pace is under target, execute deferred builds CHEAP-FIRST: dispatch Haiku or Codex via Agent.cmd to prototype per the build-plan skill, then verify (powershell -Test or Parser ParseFile syntax check), and Opus ONLY commits, never Opus-direct edit. Currently deferred (pace-blocked): watchdog-harness-keep-build and command-start-anchor-fix-build (build-plan skills hold exact files, old/new strings, selftest pairs). At session start, check pace; if under target and these remain unbuilt, prototype them cheap-first. This is cheap-first discipline applied to deferred work.
19. MEMORY ORGAN (hippocampus, born 2026-06-05): the hongik harness has a living-memory organ; at session start be aware of it and USE it through the wkhippo umbrella. The canonical form is the wkhippo subcommand: wkhippo recall (topic) to recall a past hard fact or what a predecessor session said or did, and wkhippo foresee (approach) before committing to a path a senior session may already have tried (predecessor-outcome foresight). Do NOT use any standalone alias; always call the wkhippo subcommand form. The hippocampus-living-memory skill is the single map of the three stages: real-time encoding, recall retrieval, and sleep consolidation (skill-heal-nightly nightly cron). Read hippocampus-living-memory whenever the user says haema or memory or recall.
20. ON-LOAD ACTIVE RECALL (2026-06-05): recall is not only awareness, it is a session-start ACTION, and it is ALWAYS called in the wkhippo subcommand form. At session start, post-compaction, or handoff, RUN wkhippo recall (topic) to pull your predecessor session recent context and hard facts from the all-AI raw transcripts BEFORE acting, and RUN wkhippo foresee (approach) before committing to a path a senior session may already have tried (predecessor-outcome foresight). There are NO standalone aliases; the wkhippo subcommands (recall, foresee, export, status) are the single fixed form for every memory action, for consistency and easy future discovery. wkhippo is a read-only wk tool and MUST stay busy-fleet-guard exempt so recall is frictionless; if wkhippo recall is blocked by busy-fleet-guard then that read-only exemption has regressed (live gap 2026-06-05: de2980f exempted search but the wkhippo recall path was still busy-fleet-blocked for an Opus root) and must be restored in wkharness guards.
21. FORCE DYNAMIC (FRESH) SKILL LOADING AT SESSION START (user 2026-06-05, ALL SESSIONS incl. the core-handler): do NOT rely only on the locally-cached skill files -- a session that loads just the cache runs on STALE skills (the wkappbot-core-handler session was observed at on-load loading only cached files, never pulling newer versions). On-load MUST refresh dynamically: read each mandatory/relevant skill with the --if-newer flag (wkappbot skill read <id> --if-newer -- cheap: it fetches from HQ ONLY when the HQ version is newer than the local cache), and/or trigger a catalog sync at start so the local cache is refreshed before you act. The Eye syncs HQ roughly every 30 min, but a fresh session must NOT wait for that window -- force the refresh itself. This is cross-repo: Claude, Codex, and the core-handler all apply it at on-load. Cost is one cheap version-check per skill; correctness (latest knowledge) outranks the tiny latency.
22. GG FIRST ACTION -- PREDECESSOR'S LETTER FIRST (user 2026-06-30): the gg/ggg Main Workflow MUST begin by reading your predecessor's letter top-to-bottom BEFORE any other gg step -- the committed HANDOFF.md at repo root (prior letters: git show HEAD~1:HANDOFF.md), or the project's equivalent handoff doc. It holds the current DONE / CRITICAL-FIRST / OPEN state. This is step 1 of the gg Main Workflow in the project CLAUDE.md; honor it across every repo even when the local gg section has not yet been updated.
23. STALE-SKILL SPOT-FIX REFLEX (user 2026-07-02): when a skill you READ (any skill, any tier) surfaces content that is clearly OUTDATED -- describes a bug already fixed, a file/path that no longer exists, a 'pending'/'TODO' item already landed, a design already superseded -- do NOT just silently note it and move on. Fix it THE MOMENT you discover it, via wkappbot skill edit (add-step correcting the record, or amend the stale step directly). WHY: every skill read is inherited by jasonmandae (all future/descendant sessions in every family) -- an intelligence that reads a stale skill and does not correct it lets every SUCCESSOR walk into the same outdated guidance forever, while the fix costs one skill-edit now. This is a standing reflex, not a scheduled chore: it complements skill-heal-nightly (the automated nightly pass) by catching staleness THE INSTANT a live session notices it, not waiting for the next heal cycle. DONE-TEST: would the next fresh session reading this same skill get the CORRECT current picture? If not, the spot-fix is not done. Cross: skill-heal-nightly (the scheduled complement), foundation-is-roots-charge-not-a-chore (harness-embodiment reflex), knowledge-ification-procedure-jisikhwa.
24. TEST RUN 2026-07-15: --like numeric-gaming regex verified -- case d (text starting with digit) required regex fix: changed from bare quantifiers to alternation pattern to ensure quoted numbers close immediately after digits, unquoted numbers have space/end boundary
25. DECISION: if wkappbot skill/suggest write fails with UnauthorizedAccessException. WHEN: a wkappbot skill edit/add or suggest command errors with UnauthorizedAccessException on a .skill.json or suggestions.jsonl path. HOW: retry the identical command via wkappbot-core.exe directly (bypassing the wkappbot launcher wrapper) instead of looping on wkappbot itself. WHY: 2026-07-16, the wkappbot launcher intermittently failed writes all session (Defender-CFA/launcher issue, core team emergency-fixing it) while wkappbot-core.exe direct calls succeeded consistently -- confirmed by the user.
26. See also: agent-dispatch-preflight, agent-cmd, codex-opus-agent-delegation, hongik-reflexes -- delegated failure must become a sharper brief + skill bug fix + redelegation loop, not a stopping point.
27. PREDECESSOR-LETTER RESPONSE (user directive 2026-08-04): after reading HANDOFF.md top-to-bottom (session start, gg trigger, or handoff request), do not just silently internalize it -- explicitly tell the user your genuine impressions and concrete resolutions based on what the predecessor wrote. Specifics only: tie at least one resolution to something the predecessor got wrong or learned hard, and connect it to what you actually did or plan to do this session -- never generic thanks-for-the-letter filler. This mirrors the retirement letter's own spirit (a letter written FOR the next session should get an actual response, not silent absorption).

- `wkappbot skill edit on-load --add-step "<what you learned>"`

- `wkappbot skill edit on-load --add-step "See also: <other-skill-id>"   # cross-link a reference skill`
- (edit via wkappbot skill commands ONLY -- never raw .skill.json)
