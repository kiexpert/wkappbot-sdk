# Handoff 2026-03-29 Session (continued from 03-28)

## CURRENT STATE: v5.3.38+ (25+ commits today), Eye running, all pushed

## NEEDS TESTING (user terminal, not bash IOCP)
1. `wkappbot windows "chrome"` — sibling windows section should appear
2. `wkappbot windows` — column header alignment
3. Screensaver fade-in from transparent + instant dismiss on input
4. Console telepathy: `wkappbot a11y type "*TELEPATHY*" "echo hello"`

## KNOWN ISSUES
1. bash→Launcher→IOCP path: 30s delay + output loss (NOT a regression, pre-existing)
2. windows sibling feature: 2-pass logic added but untested via Eye pipe
3. CMD-MCP moved to stderr — may need reverting if Eye pipe breaks

## TODAY'S CHANGES (commits on main, all pushed)
### File splits
- A11yCommand 2935→6, AppBotEyeGlobalMode 1953→4, InspectionCommands 2639→4
- FileToolCommands 1598→4, McpCommand 1508→3, InputCommand 2341→5
- Launcher Program.cs split (5 files from previous session, committed)

### InputCommand refactoring
- Triad debate → mutable class + enum result consensus
- 12 methods extracted (Focusless/Physical/Legacy)
- Orchestrator replaces 2200-line if-chain

### Smart stderr
- ErrorScope: console-direct only (piped processes NEVER buffer stderr!)
- AppBotExit() universal exit helper
- PulseStep silent by default (WKAPPBOT_PROFILE=1)

### Ask MD recording
- .wkappbot/ask/{timestamp}-{ai}-{slug}.md
- Triad: real-time append mode
- SlackPostToThread hook → all Slack messages in MD

### LAUNCH JSON
- Stealth: field-by-field \r (no wrap possible)
- Process chain + console + foreground hwnd
- Prof() logs full JSON (WKAPPBOT_PROFILE=1)

### Console telepathy (A11yActions.Type.cs)
- Classic console: WM_CHAR PostMessage (focusless!)
- ConPTY terminal: clipboard PostMessage Ctrl+V (focusless)
- Electron (VS Code): clipboard keybd_event Ctrl+V + STA thread
- Tested: classic console ✅, VS Code ✅

### CreateProcessW CWD fix
- All calls pass Environment.CurrentDirectory (was null → system32)
- Traced wrapper logs all args via Prof()
- Fixed in: Launcher (5 calls) + CLI EyeMcpClient (1 call)

### Screensaver fixes
- Per-monitor visual update (was global _lastT → only first monitor)
- Starts transparent (wasHidden path separated)
- Self-terminates on user input (ShouldExit flag)
- Instant dismiss: Invoke (sync) instead of BeginInvoke

### Windows command improvements
- Columnar output (class name removed, flags compressed to H/L/T/↑/N/M/D)
- Column header line
- ⌨key: label for keyboard focus (hidden when focus=self)
- Sibling windows of matched process (2-pass)
- FindAllPrompts cooldown 1s→10s (MCP worker availability)

### MCP improvements
- WriteJsonRpc smart flush
- ErrorScope removed from MCP handler (piped process!)
- --core <path> launcher option

## REMAINING WORK
1. Windows sibling display — test via Eye pipe, may need debugging
2. 30s cold start — separate sample test (don't touch production!)
3. Chrome tabs via CDP (--tabs option)
4. Process-grouped window view
5. VS Code terminal focusless typing (currently needs focus for Ctrl+V)
6. Launcher reload command (hot-reload without Eye restart)

## KEY FILES CHANGED
- csharp/src/WKAppBot.CLI/Commands/A11yActions.Type.cs — console telepathy
- csharp/src/WKAppBot.CLI/Commands/InspectionCommands.Windows.cs — format + sibling
- csharp/src/WKAppBot.CLI/ScreenSaverOverlay.cs — per-monitor + self-terminate
- csharp/src/WKAppBot.CLI/Commands/McpCommand.ErrorBuffer.cs — ErrorScope + AppBotExit
- csharp/src/WKAppBot.CLI/Commands/AskCommands.MdWriter.cs — ask MD recording
- csharp/src/WKAppBot.CLI/Commands/AskCommands.Triad.cs — live MD append
- csharp/src/WKAppBot.CLI/StepProfiler.cs — PulseStep silent default
- csharp/src/WKAppBot.Launcher/Program.cs — LAUNCH JSON + CreateProcessW trace

## GOLDEN BINARY
W:\SDK\bin\wkappbot.hq\wkappbot-core.known-good-v5.3.21.exe
