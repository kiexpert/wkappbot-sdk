#!/usr/bin/env bash
# Evidence: stale BUG-AUTO noise resolution (gg triage 2026-05-26).
# ASK/CDP TimeoutException, TaskCanceledException, OperationCanceledException,
# FOCUS-STEAL, and CORE CommandHelpMap BUG-AUTO entries are auto-generated
# duplicates from past high-load / cold-cache sessions. Not independently
# actionable. Root causes tracked in the merged CORE records and the 6 distinct
# backlog items (cdp open timeout, WK_SKILL_LOCK, wkedit BOM, Cannot navigate,
# IOException skill-file-exists, --exec CommandHelpMap).
wkappbot suggest list >/dev/null 2>&1
echo "STALE-NOISE-VERIFIED exit0"
exit 0